[project]
name = "langchain-products"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asttokens>=3.0.1",
    "groq>=1.0.0",
    "openai>=2.16.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
]
